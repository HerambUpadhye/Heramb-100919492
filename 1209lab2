# 1209lab2
FROM node:20-slim

# Create app directory
WORKDIR /usr/src/app

# Install dependencies
COPY package*.json ./
RUN npm install --omit=dev

# Copy application files
COPY . .

# Cloud Run will set the PORT env variable
EXPOSE 8080

# Start the server
CMD [ "node", "index.js" ]
