# 1. Use the node:19.7.0-alpine base image
FROM node:19.7.0-alpine

# 2. Set the environment variable NODE_ENV with a value of production
ENV NODE_ENV=production

# 3. Create a new directory in root called labone
RUN mkdir /labone \
    # a. Change the ownership to the node user and node group
    && chown -R node:node /labone

# b. Set this new folder as the working directory
WORKDIR /labone

# 4. Set the user to node
USER node

# 5. Copy all source files and change ownership to node user and node group
COPY --chown=node:node . .

# 6. Run npm install to install node.js packages
RUN npm install

# 7. Expose port 3000
EXPOSE 3000

# 8. Set default execution command
CMD ["node", "src/index.js"]